/*describe('User Unit Tests', function(){
    var user;
    beforeEach(module('starter.services'));

    beforeEach(inject(function (_user_) {
        user = _user_;
    }));

    it('can get an instance of user', inject(function(user) {
        expect(user).toBeDefined();
    }));

    it('test user object', inject(function(user) {
       
        var oneUser = {
            username: "Sara Wiltberger",
            email: "test@gmail.com",
            password: 'password123',
            userType: 'owner',
            rating: '5',
            uniqueID:'abc123',
        };

        expect(user.get(1).username).toEqual(oneFriend.username);
    }));
});*/